import classNames from 'classnames';
import styles from './kkkk.module.scss';

export interface KkkkProps {
    className?: string;
}

/**
 * This component was created using Codux's Default new component template.
 * To create custom component templates, see https://help.codux.com/kb/en/article/kb16522
 */
export const Kkkk = ({ className }: KkkkProps) => {
    return <div className={classNames(styles.root, className, styles.xxxxcdv)}>Kkkk</div>;
};
